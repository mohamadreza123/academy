# اتصال Supabase

Project URL:
https://kproxkuhasbyapsstlbn.supabase.co

Publishable key is already configured in `index.html`.

## مدیر

نام کاربری: `admin`
رمز: `123456`

برای ورود Supabase Auth، در Authentication > Users یک کاربر بساز:
- Email: `admin@academy.local`
- Password: `123456`

سپس `supabase-schema.sql` را در SQL Editor اجرا کن.

## نکته
Publishable key قابل قرار دادن در کد فرانت‌اند است. هرگز `service_role` یا secret key را داخل فایل سایت قرار نده.
